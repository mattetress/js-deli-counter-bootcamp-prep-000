var katzDeliLine = [];

function takeANumber(lineLength, custName) {
  let line = [];
  line.push(custName);
  
  return line, 
  
  return `Welcome, ${custName}. You are number ${lineLength + 1} in line.`;
} 